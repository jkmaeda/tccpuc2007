package br.pucsp.tcc.modelo;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class Gerente extends Funcionario
{
    public Gerente() {
    }
    
    // configurar o logger
    static Log logger = LogFactory.getLog(Gerente.class);
}
