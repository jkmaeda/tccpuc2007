package br.pucsp.tcc.modelo;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class Caixa extends Funcionario
{
    public Caixa() {
    }
    
    // configurar o logger
    static Log logger = LogFactory.getLog(Caixa.class);
}
